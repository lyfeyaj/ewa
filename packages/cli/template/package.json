{
  "name": "wechat_app_sample",
  "version": "1.0.0",
  "description": "Wechat App Sample",
  "scripts": {
    "clean": "rm -rf ./dist/*",
    "start": "npm run clean && ewa start",
    "build": "npm run clean && ewa build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "ewa": "^0.1.4",
    "ewa-cli": "^0.1.4"
  }
}
